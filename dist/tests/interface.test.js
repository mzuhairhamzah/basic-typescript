describe('Interface', function () {
    it('should support in typescript', function () {
        const buyer = {
            id: 1,
            name: "Zuhair"
        };
        console.info(buyer);
    });
});
export {};
